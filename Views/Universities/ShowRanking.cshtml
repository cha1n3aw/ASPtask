@model IEnumerable<ASPtask.Models.University>

@{
    ViewData["Title"] = "University Ranking";
}

<h1>University Ranking</h1>
<table class="table">
    <thead>
        <tr>
            <th>
                Rank
            </th>
            <th>
                University
            </th>
            <th>
                Points
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @{
            int c = 0;
            foreach (var item in Model.OrderByDescending(university => university.UniversityPoints))
            {
                c++;
                <tr>
                    <td>
                        @c
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UniversityName)
                    </td>
                    <th>
                        @Html.DisplayFor(modelItem => item.UniversityPoints)
                    </th>
                </tr>
            }
        }
    </tbody>
</table>
